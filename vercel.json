{
    "version": 2,
    "builds": [
        {
            "src": "api/index-simple.ts",
            "use": "@vercel/node"
        },
        {
            "src": "api/debug.ts",
            "use": "@vercel/node"
        },
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist/public"
            }
        }
    ],
    "rewrites": [
        {
            "source": "/api/debug",
            "destination": "/api/debug.ts"
        },
        {
            "source": "/api/(.*)",
            "destination": "/api/index-simple.ts"
        },
        {
            "source": "/assets/(.*)",
            "destination": "/assets/$1"
        },
        {
            "source": "/(.*\\.(js|css|ico|png|jpg|svg|json))",
            "destination": "/$1"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "headers": [
        {
            "source": "/(.*)",
            "headers": [
                {
                    "key": "x-vercel-skip-toolbar",
                    "value": "1"
                }
            ]
        }
    ],
    "env": {
        "VITE_SUPABASE_URL": "${VITE_SUPABASE_URL}",
        "VITE_SUPABASE_ANON_KEY": "${VITE_SUPABASE_ANON_KEY}",
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_SERVICE_KEY": "${SUPABASE_SERVICE_KEY}",
        "SUPABASE_DATABASE_URL": "${SUPABASE_DATABASE_URL}",
        "SESSION_SECRET": "${SESSION_SECRET}"
    }
}